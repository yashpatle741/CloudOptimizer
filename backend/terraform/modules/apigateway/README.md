# API Gateway Module

This module creates an HTTP API Gateway with Lambda integration, CORS support, and auto deployment.

## What it creates

- HTTP API Gateway
- Lambda integration
- CORS configuration
- Auto-deployed default stage
