# CloudWatch Module

This module creates CloudWatch log groups with configurable retention and an optional alarm for Lambda error monitoring.

## What it creates

- CloudWatch log groups for the CloudOptimizer services
- Configurable log retention
- Optional CloudWatch metric alarm
