# DynamoDB Module

This module creates a DynamoDB table for storing CloudOptimizer scan results with server-side encryption, point-in-time recovery, and PAY_PER_REQUEST billing.

## What it creates

- DynamoDB table with PK and SK as the primary keys
- Server-side encryption enabled
- Point-in-time recovery enabled
- Production-friendly tagging
