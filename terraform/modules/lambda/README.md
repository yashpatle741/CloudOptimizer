# Lambda Module

This module creates a production-ready AWS Lambda function with configurable memory, timeout, reserved concurrency, environment variables, and tagging.

## What it creates

- Lambda function using Python 3.13 on ARM64
- IAM role attachment
- CloudWatch logging support
- Environment variable configuration
