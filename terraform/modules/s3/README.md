# S3 Module

This module creates an S3 bucket for CloudOptimizer reports with versioning, encryption, public access blocking, and optional lifecycle rules.

## What it creates

- S3 bucket for storage of generated reports
- Versioning enabled
- Server-side encryption enabled
- Public access blocked
- Optional lifecycle management rules
